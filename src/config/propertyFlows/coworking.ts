// ============================================================
// Co-working / Shared Spaces flow config — SCAFFOLD ONLY.
// ============================================================
import type { PropertyFlowConfig } from "@/engines/types";

export const coworkingFlow: PropertyFlowConfig = {
  category: "coworking",
  label: "Co-working / Shared Spaces",
  order: [],
  fields: {},
  questions: {},
  rules: [],
};

export default coworkingFlow;
