// ============================================================
// Agricultural Lands flow config — SCAFFOLD ONLY.
// ============================================================
import type { PropertyFlowConfig } from "@/engines/types";

export const agricultureFlow: PropertyFlowConfig = {
  category: "agriculture",
  label: "Agricultural Lands",
  order: [],
  fields: {},
  questions: {},
  rules: [],
};

export default agricultureFlow;
