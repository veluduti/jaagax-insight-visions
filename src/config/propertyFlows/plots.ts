// ============================================================
// Plots / Land flow config — SCAFFOLD ONLY.
// ============================================================
import type { PropertyFlowConfig } from "@/engines/types";

export const plotsFlow: PropertyFlowConfig = {
  category: "plots",
  label: "Plots / Land",
  order: [],
  fields: {},
  questions: {},
  rules: [],
};

export default plotsFlow;
