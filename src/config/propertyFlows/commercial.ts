// ============================================================
// Commercial flow config — SCAFFOLD ONLY.
// ============================================================
import type { PropertyFlowConfig } from "@/engines/types";

export const commercialFlow: PropertyFlowConfig = {
  category: "commercial",
  label: "Commercial",
  order: [],
  fields: {},
  questions: {},
  rules: [],
};

export default commercialFlow;
